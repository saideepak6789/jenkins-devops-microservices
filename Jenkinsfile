pipeline {
	agent any
	stages{
		stage('Build') {
			steps {
				echo "build"
			}
		}
		stage('test') {
			steps {
				echo "test"
			}
		}
		stage('Intergartion test') {
			steps {
				echo "Intergartion test"
			}
		}				
	}
}